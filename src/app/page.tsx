import { axiosInstance } from "@/lib/axios-instance";
import { MainContainer } from "@/main/MainContainer";

const Home = async () => {
  return (
    <div>
      <MainContainer />
    </div>
  );
};
export default Home;
